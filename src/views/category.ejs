<%- include('partials/header') %>
<main>
    <h2>
        <%= category.name %>
    </h2>

    <ul>
        <% projects.forEach(project=> { %>
            <li>
                <%= project.title %>
            </li>
        <% }); %>
    </ul>
</main>

<%- include('partials/footer') %>